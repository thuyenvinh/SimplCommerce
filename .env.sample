# Copy to .env and fill in. compose.yaml reads this automatically.

# SQL Server sa password — 8+ chars, mixed case, digit, symbol
SQL_SA_PASSWORD=ChangeMe!Strong1

# JWT signing key — MUST be 32+ bytes for HS256
JWT_SIGNING_KEY=dev-only-change-me-32-bytes-minimum!!
