@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        var returnUrl = Uri.EscapeDataString("/" + Nav.ToBaseRelativePath(Nav.Uri).TrimStart('/'));
        Nav.NavigateTo($"/login?returnUrl={returnUrl}", forceLoad: true);
    }
}
